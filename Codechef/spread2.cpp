// rishav.io

#include <iostream>
#include <vector>
#include <string>
#include <climits>
#include <map>
#include <set>
#include <algorithm>
#include <queue>

using namespace std;

int main() {

    ios_base::sync_with_stdio(false);
    cin.tie(0);

#ifdef __APPLE__
    freopen("input.txt", "r", stdin);
#endif

    int T; cin >> T;
    while (T--) {
         N; cin >> N;
        vector<int> vec(N);
        for (int i = 0; i < N; i++)
            cin >> vec[i];
        queue<int> q;
        LL total = 1 + vec[0];
        int day_count = 1;

        for (int i = 1; i < total; i++) {
            q.insert(i);
        }
        
        while (true) {
            if (total >= N) break;
        }
    }

}
