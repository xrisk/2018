// rishav.io

#include <algorithm>
#include <climits>
#include <cmath>
#include <iostream>
#include <map>
#include <queue>
#include <stack>
#include <string>
#include <vector>

using namespace std;

typedef long long LL;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);

#ifdef __APPLE__
  freopen("input.txt", "r", stdin);
#endif

  LL la, ra, ta;
  LL lb, rb, tb;

  cin >> la >> ra >> ta;
  cin >> lb >> rb >> tb;

  LL diff = abs(la - lb);
  LL diff2 = abs(ta - tb);

  if (diff2 == 0) {
    //
  } else if ()
}
